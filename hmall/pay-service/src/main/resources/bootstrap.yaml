spring:
  application:
    name: pay-service # 服务名称
  profiles:
    active: local
  cloud:
    nacos:
      server-addr: 192.168.253.136:8848 # nacos地址
      config:
        file-extension: yaml # 文件后缀名
        shared-configs: # 共享配置
          - dataId: shared-log.yaml # 共享日志配置
          - dataId: shared-swagger.yaml # 共享日志配置
          - dataId: shared-seata.yaml
          - dataId: shared_MQ.yaml